<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Only network access is requested at install time. Anything else
         (notifications, microphone for future voice entry, etc.) should be
         requested at the point of use, per docs/android.md's permissions
         guidance - none of those features exist in this "Now"-scoped build,
         so no other permission is declared. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AnumApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@style/Theme.Anum"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AppAuth's redirect-capture activity. The scheme here must match
             build.gradle.kts's appAuthRedirectScheme manifest placeholder
             and AuthConfig.REDIRECT_URI exactly, or the system browser has
             nowhere to hand the authorization result back to. -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="${appAuthRedirectScheme}" android:host="oauth2redirect" />
            </intent-filter>
        </activity>
    </application>
</manifest>
